<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TruckingTmsMaui.App.ViewModels"
             xmlns:core="clr-namespace:TruckingTmsMaui.Core.Entities;assembly=TruckingTmsMaui.Core"
             x:Class="TruckingTmsMaui.App.Pages.CustomersListPage"
             Title="{Binding Title}">

    <ContentPage.BindingContext>
        <vm:CustomersListViewModel />
    </ContentPage.BindingContext>

    <Grid RowDefinitions="Auto, *" Margin="20">
        
        <!-- Toolbar & Actions -->
        <HorizontalStackLayout Grid.Row="0" Spacing="10" Margin="0, 0, 0, 20">
            <SearchBar Placeholder="Search Customer or Broker..." WidthRequest="300"/>
            <Button Text="New Customer" 
                    Command="{Binding CreateNewCustomerCommand}"
                    BackgroundColor="#006400" 
                    TextColor="White"
                    CornerRadius="8"/>
            <Button Text="View Client Profiles" 
                    Command="{Binding ViewClientProfileCommand}"
                    BackgroundColor="#007BFF" 
                    TextColor="White"
                    CornerRadius="8"/>
        </HorizontalStackLayout>

        <!-- Customer List -->
        <CollectionView Grid.Row="1" ItemsSource="{Binding Customers}" SelectionMode="Single"
                        SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}">
            <CollectionView.Header>
                <!-- Table Header -->
                <Grid ColumnDefinitions="*, 2*, 1.5*, 1.5*" BackgroundColor="#E0E0E0" Padding="10">
                    <Label Grid.Column="0" Text="ID" FontAttributes="Bold" TextColor="#333"/>
                    <Label Grid.Column="1" Text="Name" FontAttributes="Bold" TextColor="#333"/>
                    <Label Grid.Column="2" Text="Billing Terms" FontAttributes="Bold" TextColor="#333"/>
                    <Label Grid.Column="3" Text="Profiles" FontAttributes="Bold" TextColor="#333"/>
                </Grid>
            </CollectionView.Header>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="core:Customer">
                    <!-- Table Row -->
                    <Grid ColumnDefinitions="*, 2*, 1.5*, 1.5*" Padding="10" HeightRequest="40">
                        <Label Grid.Column="0" Text="{Binding Id}" VerticalOptions="Center"/>
                        <Label Grid.Column="1" Text="{Binding Name}" VerticalOptions="Center"/>
                        <Label Grid.Column="2" Text="{Binding DefaultBillingTerms}" VerticalOptions="Center"/>
                        <Label Grid.Column="3" Text="{Binding ClientProfiles.Count, StringFormat='{} {0} Profiles'}" VerticalOptions="Center"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>